import { payList } from "./payList"
import { PayBlock } from "./payblock"

export const PaySection = () => {
    return (
        <div>
            <h1>Оплата</h1>
            {/* {payList.map((index, id) => <PayBlock payData={index} key={id}/>)} */}
        </div>
    )
}